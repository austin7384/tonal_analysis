INPUTS
------
data/raw/hengel_generated/article_primary_jel_pp.dta

OPERATIONS
----------
- Drops JEL codes with few observations (A, B, P, M)
- Regresses Dale-Chall readability score on FemRatio x JEL code interactions
- Uses matrix algebra to extract marginal effects of female ratio by JEL code with confidence intervals
- Creates a coefficient plot with separate panels for the main effect and the interaction terms

OUTPUTS
-------
outputs/figures/Figure-F.1.pdf


## File Paths

| Line | Command | Path |
|------|---------|------|
| 64 | `graph export` | `~/tonal_analysis/outputs/figures/Figure-F.1.pdf` |

## REVIEW (2026-04-03)

### Notes (worth knowing)
- [NOTE] line 42: `by eq (b), sort: generate jel_id = _n if eq == "asobs"` — sort order within `eq=="asobs"` is determined by `b` (the estimated marginal effect). If two JEL codes happen to have identical point estimates, their relative ordering is non-deterministic and the JEL labels assigned to `jel_id` could swap between runs. Low risk in practice but worth knowing.

- [NOTE] line 1: No `version` statement.

---
0 error(s), 0 warning(s), 2 note(s).
